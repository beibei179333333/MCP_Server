# secrets — never commit the API token
token.txt
.env
*.token

# fleet MCP server — the real inventory holds hosts/keys/passwords
servers.json
~/.config/servers-mcp/servers.json

# generated exports
members.*
*.removed.csv
out/
exports/

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
