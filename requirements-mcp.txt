# Dependencies for the fleet management MCP server (python -m mcp_server).
# The Telegram group-export tool uses requirements.txt instead; these are separate.
mcp>=1.2
paramiko>=3.0     # SSH backend (also enables password auth); optional if you use the system 'ssh'
httpx>=0.27       # async HTTP health checks (servers_health)
